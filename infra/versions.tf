# Terraform and provider version constraints
# This file specifies the required Terraform version and AWS provider version

terraform {
  required_version = ">= 1.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"
    }
  }
}